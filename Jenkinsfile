@Library('jenkins-basic-shared-library-sample@master')_

standardPipeline {
    // release_branch = "master"
    // helm_chart_name = "dev-my-helm-chart.tgz"
    // prefix may be dev, qa, stage, verify, hotfix, prod

    helm_artifactory_url = "https://artifactory-lvn.broadcom.net/artifactory/webapp/#/home"
}